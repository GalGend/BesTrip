function TripSite(siteIndex, siteName, placeId, siteLocation){

    this.siteIndex = siteIndex;
    this.siteName = siteName;
    this.placeId = placeId;
    this.siteLocation = siteLocation;
    
}

module.exports = TripSite;